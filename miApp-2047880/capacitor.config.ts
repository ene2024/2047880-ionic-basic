import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.2047880.app',
  appName: 'miApp-2047880',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
